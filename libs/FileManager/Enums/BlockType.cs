
namespace FileManager.Enums
{
    public enum BlockType : byte
    {
        NONE = 0,
        Create = 1,
    }
    public enum Status : byte
    {
        NONE = 0,
        Active=1,
        Deleted = 13,
    }
}
